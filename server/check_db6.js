require('dotenv').config(); const mongoose = require('mongoose'); const { ShopState } = require('./dist/models'); async function run() { await mongoose.connect(process.env.MONGODB_URI); const shop = new ShopState('admin'); await shop.initialize(); console.log('CUSTOM MENU LOADED:'); console.log(shop.customMenu ? JSON.stringify(shop.customMenu).substring(0, 100) : 'NULL'); process.exit(0); } run();
